schema_version: "1"
id: loader-malformed-case
name: Loader malformed case benchmark
description: A benchmark whose manifest is valid but one case is malformed.
domain: recreation
task_type: text
metric:
  verifier: exact_match
  params:
    case_sensitive: false
version: "0.1.0"
contributor:
  name: AI-bench contributors
  contact: https://example.org
license: MIT
case_glob: "cases/*.yaml"
tags:
  - recreation
status: experimental
